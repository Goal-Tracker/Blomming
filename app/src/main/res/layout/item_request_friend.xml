<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#FFFFFF"
    android:paddingLeft="16dp"
    android:paddingTop="8dp"
    android:paddingRight="16dp"
    android:paddingBottom="8dp">


    <ImageView
        android:id="@+id/RequestColor"
        android:layout_width="31dp"
        android:layout_height="31dp"

        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:background="@drawable/profile_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/RequestBtn"
        app:layout_constraintTop_toTopOf="parent"></ImageView>

    <TextView
        android:id="@+id/RequestName"
        android:layout_width="16dp"
        android:layout_height="28dp"
        android:text="신"
        android:textColor="@color/white"
        android:textSize="18dp"
        app:layout_constraintBottom_toBottomOf="@+id/RequestColor"
        app:layout_constraintEnd_toEndOf="@+id/RequestColor"
        app:layout_constraintStart_toStartOf="@+id/RequestColor"
        app:layout_constraintTop_toTopOf="@+id/RequestColor" />


    <Button
        android:id="@+id/RequestBtn"
        android:layout_width="61dp"
        android:layout_height="18dp"
        android:layout_marginTop="12dp"
        android:background="@drawable/color_circle_btn_shape"
        android:backgroundTint="?attr/colorPrimary"
        android:fontFamily="@font/fontstyle"
        android:gravity="center"
        android:text="요청 취소"
        android:textAlignment="center"
        android:textSize="8sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/RequestColor" />


</androidx.constraintlayout.widget.ConstraintLayout>